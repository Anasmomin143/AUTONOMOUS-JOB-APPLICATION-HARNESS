---
description: Print the dashboard + top opportunities + required actions.
allowed-tools:
  - Bash(python -m harness.cli status)
---

# /status

```bash
cd "$CLAUDE_PROJECT_DIR" && PYTHONPATH="$CLAUDE_PROJECT_DIR/scripts" python -m harness.cli status
```
